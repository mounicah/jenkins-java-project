name: First Workflow
on: workflow_dispatch
  jobs:
    job-1: 
    
       runs-on: ubuntu-24.04
       steps:
         -name: print hello all
           run: echo "hello all"
           
         -name: print cpu info
           run:lscpu
           
          -name: print mem info
           run: lsmem
           
